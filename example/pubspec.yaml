name: kashida_example
description: "Example app that uses the kashida package to find and insert tatweel"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  characters: ^1.4.1
  flutter:
    sdk: flutter
  google_fonts: ^8.2.1
  kashida:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
